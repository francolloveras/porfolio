---
interface Props {
  id: string
  title: string
}

const { id, title } = Astro.props
---

<section id={id} class="scroll-my-6">
  <header>
    <h2 class="text-3xl flex items-center gap-x-2 font-semibold text-neutral-200">
      <slot name="icon" />
      {title}
    </h2>
  </header>
</section>
